<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration {
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('equipment', function (Blueprint $table) {
            $table->string('asset_id')->primary(); // كود الجهاز (الباركود)
            $table->string('model');                // موديل الجهاز
            $table->string('category');             // الفئة (مثل ميكروسكوب)
            $table->enum('status', ['Available', 'Booked', 'Maintenance'])->default('Available'); // حالة الجهاز
            $table->string('laboratory_location');  // مكان المعمل
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('equipment');
    }
};